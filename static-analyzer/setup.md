# Setup

## Dependencies
- Python 3.10+
- No external deps for core (stdlib `ast` module)

## Install
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
```

## Run
```bash
python src/analyzer.py <file.py>
```
