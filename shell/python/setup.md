# Setup

## Dependencies
- Python 3.10+
- Unix-like OS (macOS/Linux) — uses `os.fork`, `os.execvp`

## Install
```bash
python3 -m venv .venv
source .venv/bin/activate
```

## Run
```bash
python src/shell.py
```
