// Test functions

fn add(a, b) {
    return a + b;
}

fn double(x) {
    return x * 2;
}

fn greet(name) {
    print "Hello, ";
    print name;
    return 42;
}

let sum = add(10, 5);
print sum;  // 15

let result = double(7);
print result;  // 14

let val = greet("jot");
print val;  // 42
